# Richmond — registration

Acquires the registry information (recorded details) for each id using the source rules and a URL minted from the id stem. No navigation step. Fills the registry cell: the JSON, or pending, or absent.
