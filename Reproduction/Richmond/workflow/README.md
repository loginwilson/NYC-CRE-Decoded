# Richmond workflow

Open a lane folder and launch it: "go to Richmond Reproduction, workflow documentation, launch at 1x20" is one command in that folder. Enumeration is the audit, not a cycle lane; synchronization, registration and documentation are the cycle. Each cycle lane claims its slice from the cloud table (claim), fills cells (land) and clocks in once a minute (heartbeat).
