# Richmond

`RICHMOND REPRODUCTION.md` is this source's written authority. `workflow/` — the four lanes, each its own code, toggled on and off independently and configurable in width. `update/` — the board for this source: tab 1 the phase, tab 2 the lanes. Feeds `reproduction.richmond`, `reproduction.richmond_update`, `reproduction.richmond_update_lanes`.
