# Acris — enumeration

Not part of the reproduction cycle. The first step to get reproduction up, and afterwards the audit: count the source (Socrata + CRFN), compare with the database total; the difference must be 0. Never touches the cloud database; no table.
